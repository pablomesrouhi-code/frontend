# Copy to deploy.env (local only, never commit). Used by scripts if you run them manually.
# EasyPanel: open your frontend service → find "Deploy webhook" / "Redeploy URL".
EASY_PANEL_DEPLOY_URL=https://YOUR_PANEL_HOST/api/...
