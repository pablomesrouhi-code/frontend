/**
 * Bump when checkout/network copy changes.
 * Shown tiny in modal footers — if missing on nabtalabo.store, the browser/CDN is serving an old JS bundle.
 */
export const CHECKOUT_UI_REV = 'nbta-checkout-v12'
